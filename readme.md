# Templates

Install template:

```bash
cd skeleton-api
dotnet new install ./ --force
```

Use template:

```bash
dotnet new skeleton-api \
--name=PROJECT_NAME \
--api-port=API_PORT \
--force
```

Uninstall template:

```bash
dotnet new uninstall ./
```
